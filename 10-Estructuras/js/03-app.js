// Operador mayor que (>) y menor que (<)

// Este tipo de operadores nos ayuda para a comparar los valores que nosotros deseamos
// Ejemplo:

const efectivo = 1000;
const comparar = 800;


// if(efectivo >= comparar) tambien es usado para realizar que no tenga error cuando tengan el mismo valor.
if(efectivo > comparar){
    console.log("Usted puede realizar su compra");
} else{
    console.log("No puede Realizar la compra");
}

//REMEMBER Podemos Realizar la estructura como nosotros deseamos ya que es nuestra forma de poder entender más facl cuando estemos creando más código