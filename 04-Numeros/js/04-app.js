// uso de operaciones

let resultado;

// Suma simple
resultado = 30 + 20;

// Suma con multiplicacion
// Lo hace como la escuela primero las suma y despues la multiplicacion
resultado = 50 + 90 * 30;

// Para seleccionar o que se haga primera funcion debe colocarse entre paretecis ()
resultado = (50 + 90) * 30;

// Descuento 20%
resultado = (50 + 60 + 80 + 100) * .2;

// Impuesto
resultado = (500 + 8050 + 100) * 1.16;

console.log(resultado);